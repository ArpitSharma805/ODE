# 01 — Setup Project Infrastructure and Development Environment

**What to build:** Initialize the project structure with Python package configuration, virtual environment, development container, and basic tooling setup.

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] Create pyproject.toml with Python dependencies (Streamlit, FastAPI, LangGraph, etc.)
- [ ] Set up virtual environment and package installation
- [ ] Configure VS Code dev container with Python and Node.js support
- [ ] Set up pytest, mypy, and Playwright for testing
- [ ] Create basic project directory structure (src/ode, tests, apps/web)
- [ ] Configure .gitignore for Python, Node, and generated files
- [ ] Set up SQLite database connection and basic schema
