{
  "name": "ode",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:api": "PYTHONPATH=src .venv/bin/uvicorn ode.api.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir src",
    "dev:web": "cd apps/web && npm run dev -- --port 3000"
  }
}
